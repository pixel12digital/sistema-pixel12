🔧 COMANDOS PARA EXECUTAR NA VPS
================================

1. CONECTAR NA VPS:
   ssh root@212.85.11.238

2. PARAR O SERVIDOR COMERCIAL:
   pm2 stop whatsapp-comercial
   pm2 delete whatsapp-comercial

3. LIMPAR SESSÕES:
   cd /var/whatsapp-api-comercial/whatsapp-api
   rm -rf sessions/default
   rm -rf .wwebjs_auth
   rm -rf .wwebjs_cache

4. REINICIAR O SERVIDOR:
   pm2 start whatsapp-api-server.js --name whatsapp-comercial

5. VERIFICAR STATUS:
   curl http://localhost:3001/status
   curl http://localhost:3001/qr

6. VERIFICAR PROCESSOS:
   pm2 list
   netstat -tulpn | grep :3001

📋 COMANDOS RÁPIDOS (copie e cole):

# Parar e deletar
pm2 stop whatsapp-comercial
pm2 delete whatsapp-comercial

# Limpar sessões
cd /var/whatsapp-api-comercial/whatsapp-api
rm -rf sessions/default
rm -rf .wwebjs_auth
rm -rf .wwebjs_cache

# Reiniciar
pm2 start whatsapp-api-server.js --name whatsapp-comercial

# Verificar
curl http://localhost:3001/status
curl http://localhost:3001/qr 