# 🔧 EDITANDO WEBHOOK COM NANO
# Execute estes comandos em sequência

# 1. Navegar para o diretório
cd /var/whatsapp-api

# 2. Fazer backup
cp whatsapp-api-server.js whatsapp-api-server.js.backup.$(date +%Y%m%d_%H%M%S)

# 3. Abrir o arquivo no nano
nano whatsapp-api-server.js

# 4. NO NANO - Procurar pela configuração do webhook:
# Pressione Ctrl+W (buscar)
# Digite: webhookConfig
# Pressione Enter

# 5. NO NANO - Encontrar a linha que contém:
# let webhookConfig = {
#     url: 'https://app.pixel12digital.com.br/painel/receber_mensagem_ana_local.php',
#     events: ['onmessage', 'onqr', 'onready', 'onclose']
# };

# 6. NO NANO - Alterar a URL para:
# let webhookConfig = {
#     url: 'https://app.pixel12digital.com.br/webhook_sem_redirect/webhook.php',
#     events: ['onmessage', 'onqr', 'onready', 'onclose']
# };

# 7. NO NANO - Salvar e sair:
# Pressione Ctrl+X
# Pressione Y (confirmar)
# Pressione Enter

# 8. Reiniciar o serviço
pm2 restart whatsapp-3000

# 9. Testar a configuração
curl -s "http://127.0.0.1:3000/webhook/config" 