# 🔧 COMANDOS PARA CORRIGIR WEBHOOK NO SERVIDOR
# Execute estes comandos em sequência no servidor

# 1. Navegar para o diretório correto
cd /var/whatsapp-api

# 2. Fazer backup do arquivo atual
cp whatsapp-api-server.js whatsapp-api-server.js.backup.$(date +%Y%m%d_%H%M%S)

# 3. Verificar configuração atual
grep -n "webhookConfig" whatsapp-api-server.js

# 4. Corrigir a URL do webhook
sed -i "s|url: '.*'|url: 'https://app.pixel12digital.com.br/webhook_sem_redirect/webhook.php'|g" whatsapp-api-server.js

# 5. Verificar se a correção foi aplicada
grep -n "webhookConfig" whatsapp-api-server.js

# 6. Reiniciar o serviço
pm2 restart whatsapp-3000

# 7. Verificar status do serviço
pm2 status

# 8. Testar a configuração
curl -s "http://127.0.0.1:3000/webhook/config" 